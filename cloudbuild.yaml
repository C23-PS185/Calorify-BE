steps:
- name: 'gcr.io/cloud-builders/gsutil'
  args:
  - 'cp'
  - 'gs://calorify-bucket/.env'
  - '${_WORKING_DIR}/.env'

- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - 'app'
  - 'deploy'
  timeout: '600s'
  dir: './'

options:
  logging: 'CLOUD_LOGGING_ONLY'
